[project]
name = "gem5-trace-convert"
version = "0.1.0"
description = "Convert compressed gem5 Perfetto traces to .pftrace format"
requires-python = ">=3.8"
dependencies = [
    "perfetto>=0.13.1",
]
